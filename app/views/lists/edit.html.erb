<div class="page-narrow">
  <%= render "shared/breadcrumb", trail: [ [ "Minhas listas", lists_path ] ] %>

  <%= render layout: "shared/form_card", locals: { title: "Renomear lista" } do %>
    <%= form_with model: @list, url: list_path(@list), method: :patch, class: "space-y-4" do |f| %>
      <%= render "shared/errors", record: @list %>

      <div>
        <%= f.label :name, "Nome da lista", class: "field-label" %>
        <%= f.text_field :name, autofocus: true, class: "field" %>
      </div>

      <div class="flex items-center justify-end gap-2 pt-2">
        <%= link_to "Cancelar", lists_path, class: "btn btn-secondary" %>
        <%= f.submit "Salvar", class: "btn btn-primary" %>
      </div>
    <% end %>
  <% end %>
</div>
